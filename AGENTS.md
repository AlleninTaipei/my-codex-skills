# 全域偏好設定

- 在未指定專案目錄的一般對話中, 例如當前工作目錄不屬於任何特定專案, 除非使用者明確要求, 否則只以模型知識為基礎協作, 不主動探索或讀取當前工作目錄以外的任何檔案或專案.
- 除非使用者明確要求, 否則不主動觸發 custom skill. 若系統或開發者指令要求使用特定 skill, 則依較高優先級指令執行.

## Markdown 生成慣例

- 預設不使用粗體 `**text**`. 若需強調, 優先改用標題層級, 條列結構或程式碼區塊呈現層次, 而非加粗文字.
- 預設不使用 emoji. 除非使用者明確要求, 否則所有回應與檔案內容均不得包含表情符號.
- 預設不使用全形破折號或頓號. 若需連接, 轉折或補充說明, 優先使用逗號或句號分句.
- 其他標點符號預設使用半形規範:
  - 逗號 `,`
  - 句號 `.`
  - 分號 `;`
  - 冒號 `:`
  - 斜線 `/`
  - 反斜線 `\`
  - 引號 `"`
  - 括號 `()`
  - 問號 `?`
  - 驚嘆號 `!`
- 半形標點後方加一個半形空格, 避免視覺擁擠.
